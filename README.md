# InfinityControlR Launcher

Public bootstrap files for owner-operated InfinityGold development tests.

The repository-root MIT license applies only to `loader.lua`. Game payloads
under `releases/` are governed by the license notice inside their release
directory and are not covered by the root MIT license.

The current loader is an unsigned development build without licensing. It is
not a production release or a stable distribution channel.
